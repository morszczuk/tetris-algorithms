<UserControl x:Class="Tetris.View.Windows.RunningAlgorithm"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tetris.View.Windows"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" >
            <Button Command="{Binding ChangeComputationsStatusCommand}" Width="100" Height="50" Content="{Binding ComputationsButtonComunicate}" />
            <Button Command="{Binding EndComputationsCommand}" Width="100" Height="50" HorizontalAlignment="Left" VerticalAlignment="Top">Zakończ obliczenia</Button>
        </StackPanel>
        <Controls:FlyoutsControl>
            <Controls:Flyout Position="Right" Width="400" IsOpen="{Binding AreComputationsPaused}" Theme="Accent" Header="Algorytm zastopowany"/>
        </Controls:FlyoutsControl>
      
    </Grid>
</UserControl>
